{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/kingdom-ai-agents/image-selector-input.schema.json",
  "title": "Image selector agent input",
  "description": "Input JSON for ImageSelectorAgent._load_input. Same structure as stock image search agent output (theme + slides with title, content_text, image_url, and an images array of stock hits). Unknown root properties are ignored at runtime.",
  "allOf": [
    { "$ref": "../stock_image_search/output.schema.json" }
  ]
}
